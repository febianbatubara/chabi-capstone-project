<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chabi.android.chabiapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChaBiApp">
        <activity android:name=".ui.detail.PersonalityDetailActivity"></activity>
        <activity android:name=".ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.detail.DetailActivity" />
        <activity android:name=".ui.home.MainActivity" />
        <activity android:name=".ui.questionnaire.QuestionnaireActivity" />
        <activity android:name=".ui.starting.StartingActivity" />
        <activity android:name=".ui.starting.SignInActivity" />
    </application>

</manifest>